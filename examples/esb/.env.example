# copy to .env and fill in

# port the connector listens on; PORT takes precedence when both are set
CONNECTOR_PORT=4100

# the bearer Atlas presents on every POST; under 32 characters the server refuses to boot (openssl rand -hex 24)
ATLAS_CONNECTOR_TOKEN=

# no ESB credentials here: each tenant sends its own, in the credentialSchema src/capability.ts declares
