{
  "name": "atlas-connector-example",
  "version": "0.1.0",
  "description": "Brightline CRM — reference Atlas external connector built on the SDK",
  "type": "module",
  "private": true,
  "scripts": {
    "seed": "bun run scripts/seed.ts",
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@futurity/atlas-connector": "file:.."
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
