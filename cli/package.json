{
  "name": "create-atlas-connector",
  "version": "0.2.0",
  "description": "Scaffold a Futurity Atlas external connector from the sql or rest starter.",
  "type": "module",
  "license": "MIT",
  "bin": { "create-atlas-connector": "bin/create-atlas-connector.ts" },
  "files": ["bin", "templates"],
  "scripts": {
    "check": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@clack/prompts": "1.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  }
}
