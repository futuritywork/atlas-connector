# copy to .env and fill in

# port the connector listens on
CONNECTOR_PORT=4100

# the bearer Atlas presents on every POST. MUST be at least 32 characters — the server refuses
# to boot otherwise. generate one with: openssl rand -hex 24
ATLAS_CONNECTOR_TOKEN=replace-with-a-random-secret-of-32-or-more-chars

# no upstream credentials here: each tenant sends their own on every request, in the shape
# src/capability.ts declares as credentialSchema
